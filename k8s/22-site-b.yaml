apiVersion: v1
kind: ConfigMap
metadata:
  name: site-b-config
  namespace: site-b
  labels:
    app: site-b
data:
  index.html: |
    <html>
      Welcome to Site B!
    </html>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: site-b-deployment
  namespace: site-b
  labels:
    app: site-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: site-b
  template:
    metadata:
      labels:
        app: site-b
    spec:
      containers:
        - name: site-b-container
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          configMap:
            name: site-b-config
---
apiVersion: v1
kind: Service
metadata:
  name: site-b-api
  namespace: site-b
  labels:
    app: site-b
spec:
  type: ClusterIP
  selector:
    app: site-b
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: site-b-ingress
  namespace: site-b
spec:
  ingressClassName: nginx
  rules:
    - host: "site-b.localhost"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: site-b-api
                port:
                  number: 80